============
Installation
============

At the command line::

    $ pip install outpost24hiabclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv outpost24hiabclient
    $ pip install outpost24hiabclient

Or, if you are using pipenv::

    $ pipenv install outpost24hiabclient
